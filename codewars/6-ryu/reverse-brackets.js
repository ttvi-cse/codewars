/*
string s letters and brackets.
reverse strings in each pair of matching brackets
start from the innermost
ex;
- s = "a(bc)de" -> "abcde"
sol:
using regex remove or /()/g //wrong
using array to get all element
start from the innermost reverseing -> outer most
 */
function reverseParentheses(s) {

    return '';
}
console.log(reverseParentheses("a(bcdefghijkl(mno)p)q"));